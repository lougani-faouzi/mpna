#include <stdio.h>
#include <math.h>
#include "matrice.h"

int main()
{
         // matrice *mat =matrix_aI(4, 1);
          matrice *vec =lire_vector(4);
      
       // AfficheMatrice(vec);
       
       // switch (argc)
	//{
        //  case 2: 
                 AfficheVecteur(vec);
      		 //AfficheMatrice(vec);
      		 //desaloc_mat(vec);  
      		// AfficheVecteur(vec);
               // desaloc_vec(vec);
       //   case 3:
          
          
       //   break;

       //  default : printf("\n neant");

       // } 
        
        //double dotp=dotprod_simple(vec,vec,int taille);
        
       
  return 0;
}
